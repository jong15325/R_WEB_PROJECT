<!DOCTYPE html>
<html lang="en">
<head>
    <!-- ===============================================-->
    <!--    Document Title-->
    <!-- ===============================================-->
    <title>Falcon | Dashboard &amp; Web App Template</title>
    <meta name="msapplication-TileImage" content="../../../assets/img/favicons/mstile-150x150.png">
    <meta name="theme-color" content="#ffffff">

    @await Html.PartialAsync("_CommonScriptsPartial")

</head>
<body>
    @await Html.PartialAsync("_AlertPartial")

    @RenderBody()

    @await Html.PartialAsync("_CommonLinksPartial")

    <script>
        var isRTL = JSON.parse(localStorage.getItem('isRTL'));
        if (isRTL) {
            var linkDefault = document.getElementById('style-default');
            var userLinkDefault = document.getElementById('user-style-default');
            linkDefault.setAttribute('disabled', true);
            userLinkDefault.setAttribute('disabled', true);
            document.querySelector('html').setAttribute('dir', 'rtl');
        } else {
            var linkRTL = document.getElementById('style-rtl');
            var userLinkRTL = document.getElementById('user-style-rtl');
            linkRTL.setAttribute('disabled', true);
            userLinkRTL.setAttribute('disabled', true);
        }
    </script>

    @if (!string.IsNullOrEmpty(ViewBag.AlertMessage))
    {
        <script>
            // JavaScript로 알림 창 표시
            var decodedMessage = new DOMParser().parseFromString("@Html.Raw(ViewBag.AlertMessage)", "text/html").body.textContent;
            alert(decodedMessage);

        </script>
    }
</body>
</html>